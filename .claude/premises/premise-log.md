# Premise log — what we assume is true, and what depends on it

Governed by **Constitution Article 1** (verify before you commit) and **Article 8**
(a falsified premise re-opens whatever depended on it). Every level records its
load-bearing assumptions here — not only inside its own plan — so that when one turns
out false, the system can trace *everything it affects* instead of hoping someone
remembers.

(Markdown for now because every agent reads it natively and a human can scan it. If a
tool ever needs to query it programmatically, graduate it to JSON with the same
fields.)

## How to use — four operations

1. **RECORD.** When a plan states a load-bearing assumption about an external system
   or a chosen technology, add a row with status `unverified` and list what depends
   on it.
2. **VERIFY.** Attach evidence (a spike, a PoC, or docs + a captured runtime check)
   and set the status and the date checked. External-system checks go **stale after
   ~7 days**.
3. **FALSIFY & PROPAGATE.** If a check refutes the assumption, set status
   `falsified`, then mark every item in *depended-on-by* for review and route each to
   its level's human gate (Article 8).
4. **QUERY.** Before committing a plan: does it rely on any `unverified` or stale
   premise? Before deleting or changing something: what depends on it?

**Status:** `unverified` · `verified` · `accepted-as-risk` · `falsified`
**Level:** `slice` · `feature` · `architecture`

## Premises

| id | statement (one falsifiable sentence) | level | status | evidence | checked | depended-on-by |
|----|----|----|----|----|----|----|
| PR-example-01 | External API supports pagination via ?offset=N | feature | `unverified` | none | | `feature:data-sync` |
| PR-audio-bridge-latency | A Twilio↔Gemini Live audio bridge answers within 3000ms of call answer and holds every mid-response gap ≤1500ms across 3 real scripted PSTN calls. | slice | `accepted-as-risk` | None yet — untested. This is slice Premise 3 (OPEN/ACCEPTED RISK), the load-bearing assumption this entire slice exists to test. Owner accepted proceeding to Phase 2 design unverified, in writing, 2026-08-23. Evidence will be the 3 artifact pairs at `.claude/artifacts/spikes/voice-intake-demo-bridge-latency-call<N>-<date>.jsonl` + `.turnlog.txt`. | | `feature:conversation`, `slice:voice-intake-demo` exit criterion, and every deferred slice built on the bridge: caller-type routing (item 1), compliance guardrail (item 2), disclosure line (item 3), post-call dispatcher (item 4), Calendly (item 5) |
| PR-audioop-unavailable | The project runs on Python ≥3.13, where `audioop` is removed, so μ-law reference values cannot be generated at test time and must be hardcoded from the ITU-T G.711 standard. | slice | `verified` | Runtime check on this machine: `python3 -V` → 3.14.4; `import audioop` → ModuleNotFoundError; `import audioop_lts` → ModuleNotFoundError. Re-verified 2026-08-24 on the now-pinned project interpreter (`uv run python` → CPython 3.13.15): both `audioop` and `audioop_lts` raise ModuleNotFoundError. `audioop` was **removed** in 3.13 per PEP 594 (deprecated since 3.11), not merely deprecated — so the premise holds on 3.13 and 3.14 alike. **Falsification condition:** pinning the project back to Python ≤3.12 makes this row false; on falsification, flag every item in depended-on-by for review per Article 8. Q2's *decision* is unaffected either way (hardcoded ITU-T constants beat audioop-derived ones regardless — the standard is the standard); only its stated rationale shifts from "forced" to "deliberately chosen". | 2026-08-24 | `slice:voice-intake-demo` Q2 (μ-law hand-implementation), Q8 (reference-value source), Seam 1 test `test_mulaw_codec_reference_table` |
| PR-soxr-streaming-api | `soxr.ResampleStream(in_rate, out_rate, num_channels, dtype='int16')` exposes `.resample_chunk(x, last=False)` and retains filter history across calls on the same instance. | slice | `verified` | Runtime check: `scripts/verify_soxr_premise.py` against soxr 1.1.0 / numpy 2.5.2 on CPython 3.13.15. Real signature is `__init__(self, in_rate, out_rate, num_channels, dtype='float32', quality='HQ', vr=False)` — `num_channels` accepted, `channels` rejected with TypeError, `dtype='int16'` accepted and output dtype is int16 with no cast. `resample_chunk(self, x, last=False)` confirmed. Statefulness confirmed strongly: one reused instance tracks a whole-array reference at RMS 2.2e-5 (8k→16k) and 2.1e-5 (24k→8k), while a fresh instance per 160-sample chunk emits **nothing at all** (0 samples total across 50 chunks). | 2026-08-24 | `slice:voice-intake-demo` Q1, Seam 2 test `test_resampler_statefulness_across_chunks` |
| PR-resampler-emits-per-chunk | Each `InboundResampler`/`OutboundResampler` `.push()` call returns audio bytes for the frame it was just given, so one inbound frame yields one forwardable payload. | slice | `falsified` | **FALSIFIED** by `scripts/verify_soxr_premise.py`. soxr batches internally and emits on its own schedule: at 8000→16000 with real 160-sample Twilio frames, **41 of 50 chunks returned 0 samples**, the other 9 returned 1660 each. At 24000→8000 with 480-sample chunks, **34 of 50 returned 0**, the rest 489–490. Nothing is lost — streamed total plus `last=True` flush equals the whole-array reference exactly (16000 and 8000 samples) — but output is heavily quantized in time, and without a flush 1060 samples (~66ms) inbound / 170 samples (~21ms) outbound stay stuck in the buffer at call end. | 2026-08-24 | **Review flags CLEARED 2026-08-24T20:14Z** by the Q9/Q10/Q11 fold-in (see escalations). Was: `handle_twilio_frame`, `handle_gemini_chunk` (both forwarded and recorded unconditionally), Exit criterion mid-response-gap methodology, Critic note 1. All four addressed; superseded by `PR-resampler-empty-push-is-noop`. |
| PR-resampler-empty-push-is-noop | `InboundResampler`/`OutboundResampler` `.push()` may legally return zero bytes, and both handlers treat that as a no-op: no forward, no timing event — so every `frame_forwarded_to_gemini` / `chunk_forwarded_to_twilio` record in the artifact corresponds to a real send. | slice | `unverified` | Replacement for the falsified `PR-resampler-emits-per-chunk`. Owner-ratified as Q9 (empty-push contract) and Q10 (teardown flush) on 2026-08-24. **Resampler half PROVEN 2026-08-24** in `tests/test_resampler.py` (13 tests green, both directions): (a) `test_push_returns_empty_for_some_chunks_and_audio_for_others`; (b) `test_flush_recovers_every_stranded_sample` — asserts exact sample count, since streaming output is not byte-identical to a one-shot resample; (c) `test_fresh_instance_per_chunk_yields_total_silence`. Mutation-checked by hand: reintroducing the stateless bug in `Resampler.push` fails (a), (b), (c) **and** the statefulness test in both directions — 8 of 13 — confirming these assertions detect the specific defect rather than merely passing. **Handler half NOT yet proven** — "both handlers treat empty as a no-op" needs `BridgeSession`, which is not built. Row stays `unverified` until then. | 2026-08-24 | `slice:voice-intake-demo` Seam handlers, `BridgeSession.close()`, Exit criterion gap methodology (event stream must contain only real forwards) |
| PR-soxr-streamed-output-not-bit-reproducible | Two `soxr.ResampleStream` instances given byte-identical input produce byte-identical output. | slice | `falsified` | **FALSIFIED 2026-08-25** by direct measurement during Seam 4 T1. Over 5 runs of a 6-frame inbound fixture (8k→16k, real 160-byte Twilio frames): **735–766 of 1660 samples differ, max 2 LSB, RMS 0.7 LSB** (~2.1e-5 of full scale, ≈ −90 dBFS). A **single-chunk** push *is* reproducible — the divergence appears only once more than one chunk is streamed, which is why no earlier probe caught it. Ruled out by test: input-array aliasing (`.copy()` on every input changes nothing) and dtype-dependent dither (float32 single-chunk is reproducible too). **No audio or exit-criterion risk** — the deviation is two orders of magnitude below the mu-law quantization the same signal already carries on the Twilio leg. **Falsification condition (standing rule, not a future check):** byte-exact comparison of resampled audio across two separate `ResampleStream` instances is invalid, and **any test asserting it is wrong regardless of whether it currently passes** — a single-chunk fixture will pass such an assertion and a streamed one will not. Compare sample *counts* exactly and sample *values* at `MAX_RMS_ERROR = 1e-3` of full scale, which still discriminates by ~400x. This row is `falsified` rather than `verified` because the statement is written as the assumption that was disproved, per Article 8's form. | 2026-08-25 | `slice:voice-intake-demo` Premise 6, Seam 2 amendment (b) wording, Seam 4 test `test_handle_twilio_frame_forwards_resampled_audio`; any future test comparing resampled audio |
| PR-soxr-int16-default-is-silent-garbage | Leaving `soxr.ResampleStream` at its `dtype='float32'` default while pushing raw PCM16 samples causes soxr to misinterpret them as float amplitudes, producing audio garbage. | slice | `falsified` | **FALSIFIED 2026-08-25** by direct measurement. soxr type-checks the input array against the stream dtype and raises `TypeError: Input should be a `np.ndarray` with matching dtype for ResampleStream(float32).` on the **first** `resample_chunk` call. There is no silent misinterpretation and no garbage audio — the failure is loud and immediate. **Q1's decision (pin `dtype='int16'`) is unaffected and remains correct**; only its stated rationale was wrong, and it was wrong in the *safe* direction (the real failure mode is easier to catch than the one feared). Recorded because the artifact asserted a specific runtime behaviour of a third-party library without measuring it — the third such instance in this slice, after `PR-resampler-emits-per-chunk` and `PR-soxr-streamed-output-not-bit-reproducible`. **Falsification condition:** a future soxr release that coerces rather than type-checks would restore the original (feared) behaviour and make pinning the dtype load-bearing for correctness rather than for clarity; re-measure on any soxr major-version bump. | 2026-08-25 | `slice:voice-intake-demo` Q1 rationale |
| PR-asr-uk-accents | Gemini Live transcribes UK regional accents, UK postcodes, spoken monetary amounts ("four hundred and fifty thousand") and UK address lines accurately enough for intake use. | slice | `unverified` | Planned: the 3 ratified PSTN test calls carry a read-aloud segment (postcode, spoken amount, address line). Recorded as a **report observation only** — owner-ratified 2026-08-24 that this is NOT part of the slice exit criterion, which stays as ratified. | | `slice:voice-intake-demo` report; future intake slices |
| PR-sms-deliverability | A single SMS sent from a Twilio UK number with a named alphanumeric sender ID, containing two links (Calendly + form), is delivered to UK mobiles across at least two different carriers with both links intact and clickable. | slice | `unverified` | None yet — untested. Probe deliberately deferred: it is not part of the ratified bridge slice (post-call dispatcher is Deferred item 4). Requires provisioning a real Twilio number — flagged hard-to-undo in the slice plan. **Falsification of the alphanumeric-sender-ID half changes the message design, not a config value.** | | `slice:post-call-dispatcher` (not yet planned), `feature:conversation` |
| PR-advice-boundary-enforcement | The bridge refuses to state rates, affordability judgements, lender names or "rough" figures, whether asked directly, indirectly, or mid-sentence — with the regex net and the model each catching a recorded share. | slice | `unverified` | None yet — untested. Ratified grading method is manual transcript review, not an eval harness. Evidence will be a fixed probe list (direct rate ask, indirect affordability ask, lender-name ask, "just roughly", mid-sentence ask) run against the deployed line, recording regex-caught vs model-handled per probe. | | `slice:compliance-guardrail` (not yet planned, Deferred item 2), `feature:conversation`, regulatory posture of the whole product |
| PR-gemini-chunk-cadence | Gemini Live's intra-response audio-chunk delivery cadence stays small (~single-digit ms) and never rivals a genuine caller-turn-boundary gap in size. | slice | `accepted-as-risk` | None yet — untested; Gemini's docs do not document chunk cadence. This is slice Premise 5, owner-ratified as an open risk 2026-08-24 (see escalations, PREMISE_PROBE). Failure direction is a false NOT-PASSED, not a false PASS; the dangerous direction is independently closed by the exit criterion's Corroboration rule. | | `slice:voice-intake-demo` exit criterion, **gap-classification methodology only** — the "T largest gaps" turn-boundary split rests entirely on this. Not the bridge itself: if the cadence assumption is wrong, what breaks is how gaps are measured and classified, not the audio path. **Two sources of quantization, folded in 2026-08-24T20:14Z per owner direction (Q11):** (i) Gemini's own delivery cadence, still undocumented and unmeasured; (ii) soxr's batching, now measured — ~61ms bursts outbound (489-490 samples at 8kHz, roughly every third chunk), not the "single-digit ms" an earlier Exit-criterion draft assumed. Stated consequence: a genuine stall shorter than one burst interval is invisible to the gap method. Against the 1500ms threshold that is a ~24x margin, so the methodology stands unchanged — but the blind spot is recorded rather than assumed away. |

*(Add a new row whenever a plan states a new load-bearing assumption. Ensure the statement is a single falsifiable sentence. If an assumption is falsified, immediately flag all items in the 'depended-on-by' column for review.)*